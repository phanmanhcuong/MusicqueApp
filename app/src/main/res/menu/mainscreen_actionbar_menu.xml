<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/btn_search"
        android:title="@string/btn_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="android.support.v7.widget.SearchView"/>
    <item
        android:id="@+id/btn_menu"
        android:title="@string/btn_menu"
        android:icon="@drawable/ic_more_vert_black_24dp"
        app:showAsAction="ifRoom" >
        <menu>
            <item
                android:id="@+id/menu_listview"
                android:title="@string/listview_string"/>
            <item
                android:id="@+id/menu_gridview"
                android:title="@string/gridview_string"/>
        </menu>
    </item>

</menu>